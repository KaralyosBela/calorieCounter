import { PageTransition } from "../App";

//goal card, inputs, whats your dialy protein goal? and your daily calorie goal/weekly db-be mehet be, hogy friss legyen
export const GoalCard = () => {
  return (
    <PageTransition>
      <div className="h-screen">
        <div className="bg-blue-500 text-white text-9xl flex justify-center items-center h-screen">
          Welcome to this fantastic app.
        </div>
      </div>
    </PageTransition>
  );
};
